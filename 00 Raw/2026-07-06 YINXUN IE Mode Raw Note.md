# 2026-07-06 YINXUN IE Mode Raw Note

tags: #internship #raw-note #yinxun #ie-mode

## Original note, sanitized

```text
there is another note about yinxun ie mode problem. so same thing, cannot open so must set in IE mode. 
but they usually do like this:
1. check in default browser setting if it is already set to IE or not
2. check fo http or https
3. if cannot re-add again
4. if still cannot, clear cookies.
5. ONE IMPORTANT NEW THING, usually accessing yinxun we need account. PT01 and PT02 do not necessarily use the same password. The counter/櫃檯 machine was PT02, so it used a different password. If anything prompts for password, ask the user.
```

## Safety note

The original message included a password example. Do not store passwords in this vault. The cleaned note only keeps the rule: different PT machines/accounts may have different passwords, so ask the user instead of assuming.
